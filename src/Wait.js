import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const Wait = () => {
  return (
    <View>
      <Text>Wait</Text>
    </View>
  )
}

export default Wait

const styles = StyleSheet.create({})